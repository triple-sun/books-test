export enum Service {
  API = 'API',
  Prisma = 'Prisma',
  Users = 'Users',
  Books = 'Books',
}

export enum PortDefault {
  Postgres = 5432,
  Books = 3333,
}

export enum Path {
  Login = 'login',
  Register = 'register',
  Spec = 'spec',
}
